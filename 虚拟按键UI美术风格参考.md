# UI 视觉外形结构与轮廓设计规范 (Agent 专用版)

本规范定义了工业级暗色拟物化（Skeuomorphic）界面的物理结构逻辑。旨在为大模型（Agent）提供精确的几何与光影描述，以确保生成的 UI 具有一致的工业精密感。

---

## 1. 全局几何拓扑 (Global Topology)

* **容器形态 (The Shell)：** 所有功能模块均被包裹在独立的 **超长圆角矩形（Stadium Shape）** 容器底座内。
* **圆角逻辑 (Corner Radius)：**
    * **外层容器：** 圆角半径为 **4px - 6px**。
    * **内部按键：** 圆角半径为 **2px**。
    * **嵌套规则：** 遵循“等距嵌套”原则，内圆角弧度需略小于外圆角，确保轮廓平行。
* **物理厚度表现：**
    * 采用 **“双重描边”** 模拟金属切边。
    * **最外层：** 1px 纯黑 (#000000) 封闭轮廓线。
    * **次级层：** 紧贴黑线内部，顶部边缘 1px 浅灰色 (#3C3C3C) 亮线（模拟受光），底部 1px 深色暗线（模拟投影）。

---

## 2. 细分模组轮廓规范 (Modular Outlines)

### A. 凸起式连体按键组 (Raised Button Stack)
* **结构描述：** 采用 **“连续切分体”** 逻辑。多个按键共享一个长条形容器，而非相互分离。
* **物理分割线 (The Groove)：**
    * 按键间通过 **“V型槽”** 视觉隔开。
    * **构造：** 由左侧 1px 深色线 (#1A1A1A) 和右侧 1px 亮色线 (#4A4A4A) 紧贴组成，模拟板材拼接缝隙。
* **表面起伏：** * 按键表面呈现垂直线性渐变（顶亮底暗），模拟微弱的表面弧度。

### B. 下沉式槽位结构 (Recessed Slot)
* **结构描述：** 采用 **“负空间 (Negative Space)”** 建模逻辑。
* **深度感模拟 (Depth)：**
    * **上边缘 (Top Rim)：** 槽位内部顶部边缘必须带有 2px 的 **内阴影 (Inner Shadow)**，模拟物理下沉。
    * **下边缘 (Bottom Rim)：** 槽位内部底部边缘有一条 1px 的亮色反射线（Rim Light），模拟槽底捕捉到的环境光。
* **背景色：** 槽底使用纯深黑色 (#0D0D0D)，与凸起的按钮背景形成明度反差。

---

## 3. 三大状态的轮廓属性 (State Transitions)

| 状态名称 | 物理轮廓表现 | 核心视觉特征 |
| :--- | :--- | :--- |
| **静态 (Static)** | 保持外凸线性渐变。 | 顶部 1px 亮色切边清晰可见。 |
| **被按下 (Pressed)** | 轮廓向内微收缩 0.5px。 | 取消顶部亮边，整体明度降低，模拟受压下陷。 |
| **激活/高亮 (Active)** | 外部轮廓不变。 | **内部元素**（图标/文字）切换为荧光色 (#00E5FF)，并增加 1px 的等比扩散热发光 (Glow)。 |

---

## 4. 给 Agent 的结构化 Prompt (Prompt Syntax)

**指令目标：** 构建工业级精密控制面板。

1.  **容器定义：** 创建外轮廓 `border: 1px solid #000000`，内边框 `box-shadow: inset 0 1px 0 #3C3C3C`。
2.  **组件定义 (凸起)：** 使用 `linear-gradient(#4A4A4A, #383838)`，边缘采用 1px 垂直双色分割线。
3.  **组件定义 (下沉)：** 设定背景 `#0D0D0D`，添加 `box-shadow: inset 0 2px 4px rgba(0,0,0,0.8)`。
4.  **色彩规范：** 严禁使用纯扁平色块，必须通过 1px 的明暗线条表达物理边缘。