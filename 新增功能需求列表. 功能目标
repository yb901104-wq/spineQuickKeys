1. 功能目标

在现有“按键序列快捷方式软件”基础上，新增一套“桌面虚拟按键系统”。

用户可通过主窗口按钮开启/关闭虚拟按键层。
开启后，会出现一组可置顶、可交互、可绑定原快捷动作的桌面虚拟按钮。

该系统核心目标：

用虚拟按钮代替键盘实体按键
支持图形化快捷键触发
支持循环执行类按钮
支持桌面悬浮操作
支持运行时动态增减按钮
所有虚拟按钮都可作为“特殊快捷键源”接入原快捷键系统
2. 主窗口新增功能
2.1 新增按钮

在原应用主窗口新增：

按钮1：开启虚拟按键

点击后：

创建虚拟按键悬浮窗口
默认置顶
默认显示当前保存的虚拟按钮布局
若已开启则不重复创建
按钮2：关闭虚拟按键

点击后：

销毁虚拟按键悬浮窗口
停止所有循环型按钮
保存当前布局状态
3. 虚拟按键悬浮窗口
3.1 窗口特性

虚拟按键窗口需满足：

独立窗口
默认置顶
可拖动
无任务栏图标（可选）
不抢主窗口焦点
支持半透明
支持鼠标穿透（后续预留）
可保存窗口位置
3.2 UI层级

窗口包含：

虚拟按键窗口
 ├─ 虚拟按钮列表
 ├─ 空白区域
 └─ 右键菜单系统
4. 虚拟按钮系统
4.1 虚拟按钮基础定义

每个虚拟按钮属于一个独立对象：

{
  "id": "virtual_btn_001",
  "name": "按钮1",
  "styleType": "small_icon",
  "bindActionId": "",
  "loopEnabled": false,
  "loopInterval": 100,
  "loopCount": 1,
  "topMost": true,
  "iconPath": "",
  "position": {
    "x": 0,
    "y": 0
  }
}
5. 虚拟按钮样式系统
5.1 样式类型

必须支持三种样式：

类型	枚举值	描述
小图标	small_icon	小尺寸图标按钮
大图标	large_icon	大尺寸图标按钮
可循环图标	loop_icon	带循环次数输入区域
5.2 UI美术要求

必须严格遵守：

虚拟按键UI美术风格参考.md

Agent必须：

读取该文件
完全遵守其中：
色彩
圆角
阴影
动效
图标规范
悬停效果
选中状态
字体
间距
透明度
边框规则

禁止：

自行发挥UI风格
使用默认系统按钮样式
使用未定义的颜色体系
6. 右键菜单系统
6.1 空白区域右键菜单

若右键点击的是：

虚拟窗口空白区域

弹出菜单：

菜单项	功能
增加按钮	新建虚拟按钮
删除最后按钮	删除最后一个按钮
锁定置顶	设置窗口始终置顶
取消置顶	取消置顶
保存布局	保存按钮布局
重置布局	恢复默认布局
6.2 按钮区域右键菜单

若右键点击的是：

虚拟按钮本体

则菜单需增加按钮相关功能：

菜单项	功能
修改按钮样式	切换三种样式
当前绑定信息	显示绑定状态
设置快捷绑定	进入录入模式
清除快捷绑定	清除当前绑定
设置图标	更换按钮图标
删除按钮	删除当前按钮
7. 按钮绑定显示规则
7.1 无绑定时

显示：

无快捷绑定
7.2 已绑定时

显示：

当前绑定：
XXX

例如：

当前绑定：
技能连招A
8. 快捷绑定系统
8.1 核心逻辑

每个虚拟按钮都可以绑定：

原按键软件中的一个快捷动作

即：

虚拟按钮 = 特殊快捷键源
8.2 特殊快捷键机制

虚拟按钮不是键盘按键。

系统内部需要新增：

VirtualKeySource

例如：

VK_BTN_001
VK_BTN_002
VK_BTN_003

这些“虚拟按键ID”：

可参与原快捷键系统
可绑定宏
可绑定动作
不允许与键盘实体键冲突
不允许与组合键冲突
8.3 冲突检测

绑定时必须检测：

冲突类型	是否禁止
与键盘快捷键重复	禁止
与组合键重复	禁止
与其他虚拟按钮重复	禁止

若冲突：

阻止绑定
给出明确错误提示
9. 可循环图标系统
9.1 循环图标特性

loop_icon 类型按钮：

带循环次数输入框
可设置循环间隔
点击后自动循环执行绑定动作
9.2 循环次数

用户可填写：

1~9999
9.3 循环间隔菜单

右键菜单中新增：

循环间隔

子菜单：

选项	值
100ms	100
300ms	300
500ms	500
9.4 循环执行逻辑

点击循环按钮后：

for i in loopCount:
    执行绑定动作
    等待 loopInterval

要求：

不阻塞UI线程
支持中途停止
支持关闭窗口自动停止
支持并发多个循环按钮
10. 拖拽与布局
10.1 按钮拖拽

支持：

鼠标拖动按钮
自动记录位置
实时更新布局
10.2 布局保存

需保存：

{
  "windowPosition": {},
  "buttons": []
}

保存内容包括：

按钮位置
按钮类型
图标
绑定关系
循环参数
窗口状态
11. 状态持久化

应用关闭后：

自动保存配置
下次启动恢复
12. 架构要求（重要）
12.1 禁止硬编码

禁止：

按钮数量写死
菜单写死
样式写死

必须：

数据驱动
配置驱动
可扩展
12.2 推荐模块拆分
VirtualKeyManager
VirtualButtonManager
VirtualButtonWidget
VirtualButtonContextMenu
VirtualLoopExecutor
VirtualKeyBindingManager
VirtualLayoutSerializer
13. 线程要求

循环按钮执行：

必须异步
不允许卡UI
不允许阻塞主线程

推荐：

Timer
Task
Coroutine
Worker Thread
14. 错误处理要求

所有操作必须：

有异常捕获
有日志
有UI提示

包括：

绑定失败
图标加载失败
布局损坏
配置读取失败
15. Agent开发要求（重点）

AI Agent在开发时必须遵守：

15.1 不允许破坏原快捷键系统

新增系统必须：

与原系统解耦
不修改原快捷键核心逻辑
通过接口接入
15.2 所有新增功能必须模块化

禁止：

把逻辑全部写进主窗口
巨型单文件
UI与逻辑强耦合
15.3 UI必须可扩展

未来需要支持：

摇杆
长按
连点
多状态图标
动画
透明穿透
分页
触屏模式

当前架构必须预留扩展性。

16. 最终交付要求

Agent最终必须输出：

16.1 可运行功能

包括：

虚拟按键窗口
动态按钮
右键菜单
快捷绑定
循环按钮
布局保存
置顶控制


推荐实现顺序：

1. 虚拟窗口
2. 基础按钮系统
3. 右键菜单
4. 按钮样式系统
5. 绑定系统
6. 循环系统
7. 布局保存
8. UI美术适配
9. 异常处理
10. 优化与重构